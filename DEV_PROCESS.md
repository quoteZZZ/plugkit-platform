# PlugKit 开发流程与代码审查规范

> 本文件是后续所有插件开发的**硬性流程契约**：每次写代码都必须走"需求确认 → 阶段开发 → 统一验证 → 代码审查"四步，并对照下方清单逐项打分。本文件本身可随实践迭代，但修改须经使用者确认。

## 0. 产品目标（一切决策的准绳）
**快速 · 多平台 · 性能优秀 · 实用**。所有规则服务于这四点；规则与目标冲突时以目标为准。

## 1. 开发节奏（解决"速度慢 / 过度分步"）
- **阶段** = 一个完整可交付单元（一个功能，或一个插件）。非必要不拆更小。
- 阶段内**连续编码**，不每改一处就跑构建 / 类型检查 / 生成器。
- 阶段结束**统一跑一次**验证链（见第 4 节）；失败才回头，修完再统一验证，不反复试探。
- 每个阶段对应**一个本地 git 提交**，提交信息写清"做了什么 / 为什么"。

## 2. 需求确认（解决"理解错要求"）
- 动手前**复述需求 + 划边界**（做什么 / 不做什么 / 假设什么）。
- 与既有架构或产品目标冲突时，**先提出并等确认**，不擅自扩需、不擅自降级。
- 模糊点列"假设"请使用者确认，不脑补。

## 3. 写作时护栏（把问题消灭在写码时，而非只靠事后审查）
| 想做这件事时 | 先自问 |
| 提取辅助方法 | 这段逻辑会被 ≥2 处复用吗？否则内联。 |
| 加新依赖 | 它会进 content script 吗？会 → 禁止重依赖；只进 popup / options。 |
| 新建模块 / 抽象 | 现有 `plugkit-core` 或某插件已有同类能力吗？有 → 扩展而非新建。 |
| 用浏览器 API | 是 Chromium / Firefox 通用写法吗？不是 → 封进 platform 层。 |
| 加配置项 | 用户真需要调吗？不需要 → 写死默认值。 |

## 4. 验证范围（兼顾速度与质量）
默认只跑：`pnpm build` + `pnpm typecheck`。仅在以下情况追加对应验证，**不每次全量**：
- 动了消息 / 存储接口 → 跑一次端到端交互冒烟（本地加载 + 一次操作）。
- 宣称跨平台 → 额外 `pnpm build:firefox` 确认双构建通过。
- 动了 content script → 检查产物体积（content 必须 ≤ 预算，如 50KB）。

## 5. 代码审查清单（解决 5 类 AI 通病 + 守住 2 个产品目标）
每次阶段交付附《代码审查报告》。每项标**严重度**：🔴 阻断（必须修才能交付）/ 🟡 建议（改进意见）。

| # | 审查项 | 严重度 | 标准 | 不通过表现 |
|---|--------|--------|------|-----------|
| 1 | 需求符合 | 🔴 | 实现的是"复述的需求"，未超范围 | 做了未要求的 / 漏了要求的 |
| 2 | 不过度拆碎 | 🟡 | 方法按业务语义；仅 ≥2 处复用才提取 | 大量只调用一次的私有小方法 |
| 3 | 不重复造轮子 | 🔴 | 新增前先查 `plugkit-core` / 已有插件可复用 | 同一能力多处重复实现 |
| 4 | 简洁（YAGNI） | 🟡 | 最小可行；能删 30% 更好 | 预建未用抽象、嵌套过深、配置过度 |
| 5 | 兼容既有 | 🔴 | 改动前通读受影响模块，沿用约定 | 破坏 `plugkit-core` 接口 / 与示例矛盾 |
| 6 | 性能 | 🔴 | content 不引重依赖、体积达标；高频走长连接 | content 体积超标 / 同步阻塞 DOM |
| 7 | 跨平台 | 🔴 | 双构建通过；只用通用 API，差异封 platform | 仅 Chromium 能跑 / 裸用浏览器私有 API |

**审查报告模板**：
```
阶段：<名称>
改动：<清单>
审查：#1✓ #2🟡(理由) #3✓ … #7✓
改进意见：<具体>
结论：可交付 / 阻断(需修 #x)
```

## 6. 长期存储（GitHub 不可达时）
- 源码位于 `C:\Users\v1589\Desktop\PlugKit\plugkit-platform`（本机持久，**必须使用非中文/纯 ASCII 路径**）。
- **路径硬约束**：wxt(vite/rolldown) 在含中文的路径下构建会静默丢失产物（rolldown 原生写盘失败）。项目曾位于 `Desktop\插件` 时构建异常，已迁移至 `Desktop\PlugKit`。切勿将项目放入含中文的目录。
- 已初始化本地 git；定期 `git archive` 产出干净 zip 供下载。
- 回到可联网机器后用 token 推 GitHub（建议先吊销暴露过的旧 token）。

**构建产物一键同步（避免源码/产物仓库漂移）**：
- `pnpm sync-rules`：从 `apps/plugkit-bilibili/src/shared/pcdn-patterns.json`（域名单一真源）重新生成 DNR 规则 `public/rules_*.json`，并校验 `host_permissions` 覆盖。**改 PCDN 域名只改这一处 JSON，再跑本命令**。
- `pnpm sync-dist`：把各插件 `.output/chrome-mv3` 同步到仓库外同名构建产物目录（默认上一级，即 `Desktop\PlugKit`）并自动 git 提交。改 `PLUGKIT_DIST_ROOT` 可指定其他目标根。
- 流程：`pnpm -r build` 通过 → `pnpm sync-dist` 同步构建产物。

**项目盘定期同步（硬性）**：每个阶段验证通过（构建 + 类型检查全绿）后，**立即**将成果上传到项目盘对应目录，不拖延、不攒批：
- 规范 / 契约文档（`DEV_PROCESS.md`、`CONTRACT.md`）→ **需求文档**
- 干净源码 zip + git bundle（含完整提交历史）→ **开发代码**
- 可加载扩展包（`.output/*.zip`）→ **构建产物**
- 上传前先 `git archive` / `git bundle` 生成干净产物；项目盘写入属**变更操作**，执行前须使用者确认目录与文件。

## 7. 复用与一致性
- 跨切面能力沉淀进 `packages/plugkit-core`，插件只写业务。
- 新插件一律 `pnpm create-plugkit <名>` 生成，继承约定。
- 改 `plugkit-core` 接口须评估对**全部插件**兼容；此类改动标记 🔴 高风险、审查加强。
